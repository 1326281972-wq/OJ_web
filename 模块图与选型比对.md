# 模块图、模块职责表与技术选型比对

> 配套文档：《OJ在线测评系统建设方案.md》（2026-08-20）
> 本文件严格对应**第一日需求（R1~R6）与主路径**，模块/选型逐条可溯源；对不上的内容一律移除或注明处理方式。

---

## 0. 与第一日需求（R1~R6）的对应基线

| 编号 | 第一日需求 | 本项目覆盖情况 |
|------|-----------|---------------|
| R1 | 用户与权限（至少区分管理员、选手） | 覆盖 |
| R2 | 题目发布与代码提交，展示评测状态与结果 | 覆盖 |
| R3 | 供评测机调用的任务拉取与结果回传接口 | 覆盖 |
| R4 | 选手端浏览器内 WASM 编译运行 C++/Go 至少一种，展示编译错误，提交记录显示 AC/WA | 覆盖（C++） |
| R5 | 深入模块（三选一）：通信题管理 / 现场打印队列 / 客户端设备管理 | 覆盖（通信题管理） |
| R6 | 附接口说明文档，本地可启动演示 | 覆盖 |

**对不上需求的处理规则**：本模块图中只出现上表 6 项需求的模块；现场打印队列、客户端设备管理（未选）、比赛/榜单/沙箱、Go 语言等一律**不建模块**，明确移出本期范围；评测机多机分片（mod/total）仅保留为接口协议参数，不设独立模块。

---

## 1. 系统模块图

### 1.1 外部使用者

| 使用者 | 角色 | 主要动作 |
|--------|------|----------|
| 选手 | contestant | 登录、看题、编辑代码、WASM 本地运行、提交、看提交记录 |
| 管理员 | admin | 登录、发布题目、上传测试数据、管理交互器、重判 |
| 评测机 | judger（程序） | 拉取任务、检出、取源码/数据、回传结果、心跳 |

### 1.2 内部主要模块与依赖方向

```mermaid
flowchart TB
    U1[选手]:::actor
    U2[管理员]:::actor
    U3[评测机 daemon]:::actor

    subgraph 前端[前端 Vue3 SPA]
        FA[认证模块<br/>登录注册 · JWT · 路由守卫]
        FB[题库模块<br/>题库列表 · 题目详情]
        FC[编辑器+WASM 模块<br/>CodeMirror · clang.wasm 编译运行<br/>本地自测 PASS/FAIL · 交互演示]
        FD[提交记录模块<br/>状态轮询(退避) · 记录详情]
    end

    subgraph 后端[后端 FastAPI]
        BA[认证与用户服务]
        BB[题目服务<br/>题目 CRUD · 测试数据]
        BC[提交服务<br/>状态机 · 重判]
        BD[评测机接口服务<br/>任务拉取 · 原子检出 · 结果 · 心跳]
        BE[交互器服务<br/>上传 · 编译 · 版本管理]
    end

    D[(SQLite 数据层<br/>7 张核心表)]
    J1[任务拉取与结果回传]:::judge
    J2[编译 g++]:::judge
    J3[运行与比对]:::judge
    J4[交互评测 管道互联]:::judge

    U1 --> FA & FB & FC & FD
    U2 --> FA & BB & BE
    U3 --> J1

    FA --> BA
    FB --> BB
    FC --> FB
    FD --> BC
    FD --> FC
    BA --> D
    BB --> D
    BC --> D
    BD --> D
    BE --> D
    BD --> BC
    BE --> BB

    J1 --> BD
    J2 --> J1
    J3 --> J1
    J4 --> J1

    classDef actor fill:#eef2ff,stroke:#6366f1
    classDef judge fill:#ecfdf5,stroke:#10b981
```

依赖方向说明：**前端各模块只依赖后端接口**（不直连数据库）；**后端各服务依赖 SQLite 数据层**；**评测机只依赖后端评测机接口服务**（HTTP 轮询，互相解耦）。前端编辑器+WASM 模块与提交记录模块之间的虚线协作（本地自测 PASS 后可一键作为正式提交写入记录）。

### 1.3 模块如何协作完成主路径

主路径：启动后端+seed → 启动前端 → 启动评测机 → 登录 → 打开题库 → 编写代码 WASM 运行 → 提交 → 轮询状态 → 评测机评测 → 显示终态。

1. `seed 脚本 + 数据层`初始化数据库（admin/demo_user/judger1、1001 A+B、1002 猜数）；
2. 选手在 `认证模块`登录，`认证与用户服务`签发 JWT，路由守卫放行选手端；
3. `题库模块`从 `题目服务`拉取列表与详情（含样例、时限、spj 标记）；
4. 选手在 `编辑器+WASM 模块`点击"运行"：浏览器内 clang.wasm 编译→WASI 运行→显示输出/编译错误/样例比对 PASS；此步骤**不经过后端**；
5. 点击"提交"：前端调 `提交服务`建提交（status=pending）；
6. `提交记录模块`指数退避轮询 `提交服务`状态；
7. `评测机 daemon`轮询 `评测机接口服务`拉任务→原子检出→取源码/数据→`编译/运行比对`（普通题）或`交互评测`（通信题）→回传结果；
8. `提交服务`写终态，前端轮询到 accepted/wa 等并展示。

---

## 2. 模块职责表

> 配套模块图：图上每个方框在此表中有分工。负责人 = 前端/后端/评测机开发（本项目由一人独立承担，以下按端划分职责域）。

### 2.1 前端（Vue3 SPA）

| 模块 | 输入 | 输出 | 依赖谁 | 谁负责 | 对应需求 |
|------|------|------|--------|--------|----------|
| 认证模块 FA | 账号密码表单、JWT | token、用户信息、路由放行/跳转 | 后端认证与用户服务 | 前端 | R1 |
| 题库模块 FB | 题目数据（后端接口） | 题库列表、题目详情页 | 后端题目服务 | 前端 | R2 |
| 编辑器+WASM 模块 FC | 用户源码、题目样例、JS 交互器 | 编译错误/程序输出/本地 PASS-FAIL、交互对话、提交动作 | 题库模块(取样例)、静态 WASM 资源、后端提交服务(提交) | 前端 | R4/R5 |
| 提交记录模块 FD | 提交列表、批量状态查询 | 状态徽章、耗时/内存、编译错误展示、轮询停止 | 后端提交服务 | 前端 | R2 |

### 2.2 后端（FastAPI）

| 模块 | 输入 | 输出 | 依赖谁 | 谁负责 | 对应需求 |
|------|------|------|--------|--------|----------|
| 认证与用户服务 BA | 注册/登录请求、token | token、用户资料、权限校验结果 | 数据层 | 后端 | R1 |
| 题目服务 BB | 题目 CRUD、测试数据文件 | 题目列表/详情、测试数据清单 | 数据层 | 后端 | R2 |
| 提交服务 BC | 提交创建、状态查询、重判 | submission_id、状态机流转、提交列表/详情 | 数据层、认证服务 | 后端 | R2 |
| 评测机接口服务 BD | 拉取/检出/结果/心跳请求 | 任务列表、检出结果、数据文件流、终态写入 | 提交服务、题目服务、数据层 | 后端 | R3 |
| 交互器服务 BE | 交互器源码上传 | 编译产物、version+1、交互器下载 | 数据层、题目服务 | 后端 | R5 |
| SQLite 数据层 D | ORM 读写调用 | 持久化数据、查询结果 | 无（SQLite 文件） | 后端 | R1~R5 |

### 2.3 评测机（Python 演示 daemon）

| 模块 | 输入 | 输出 | 依赖谁 | 谁负责 | 对应需求 |
|------|------|------|--------|--------|----------|
| 任务拉取与回传 J1 | judger token、任务请求 | 任务列表、结果/错误信息回传 | 后端评测机接口服务 | 评测机 | R3 |
| 编译 J2 | 选手源码、交互器源码 | 可执行文件或编译错误 | 任务拉取 J1 | 评测机 | R3 |
| 运行与比对 J3 | 可执行文件、测试数据 | AC/WA/TLE/RE + 耗时内存 | 任务拉取 J1、编译 J2 | 评测机 | R3 |
| 交互评测 J4 | 选手程序 + 交互器 + 测试输入 | accepted / wrong_answer | 任务拉取 J1、编译 J2 | 评测机 | R5 |

> 交付物类（接口说明文档 docs/api.md、seed 脚本、启动脚本、README）不属于运行模块，但承担 R6 验收，由后端/前端共同维护。

---

## 3. 第一日需求/主路径 ↔ 模块 逐条对照

| 第一日核心必做 / 主路径步骤 | 对应模块 | 覆盖情况 | 不匹配处理 |
|------------------------------|----------|----------|-----------|
| R1 用户与权限 | FA + BA | 覆盖 | — |
| R2 题目发布/提交/状态展示 | FB + FD + BB + BC | 覆盖 | — |
| R3 评测机接口 | BD + J1/J2/J3 | 覆盖 | — |
| R4 浏览器内 WASM 编译运行+AC/WA | FC（本地）+ BC（正式判定） | 覆盖 | — |
| R5 通信题管理 | BE + J4 + FC 交互演示 | 覆盖 | 未选的两个深入模块（打印队列/设备管理）**移除本期范围**，不建模块 |
| R6 文档与演示 | docs/api.md + seed + 启动脚本 | 覆盖 | 作为交付物维护，不设运行模块 |
| 主路径：登录/看题/运行/提交/轮询/评测/终态 | FA→FB→FC→BC→FD→BD→J1~J4→BC→FD | 覆盖 | — |
| 方案中预留但本期不实现 | mod/total 多机分片、生产沙箱、比赛榜单 | — | **仅保留协议参数/移除**，不设模块 |

---

## 4. 技术选型比对表

> 原则：优先采用第一日方案已定技术，保证 9 天内可调试、可演示。每个技术点至少两个候选，按同一维度（环境安装便利性 / 与课题实例接近度 / 问题能否一天内定位）逐项比较。

### 4.1 前端框架：Vue3 + Vite（入选） vs React + Vite（放弃）

| 维度 | Vue3 + Vite | React + Vite |
|------|-------------|--------------|
| 环境是否便于安装 | npm 一条命令，脚手架极简 | 同样简单，但配套选型（状态/路由）多 |
| 与课题实例接近度 | 组合式 API + Pinia 状态管理直观，WASM 封装示例多 | 生态庞大，取舍成本高 |
| 问题能否一天内定位 | 单文件组件 + devtools 可逐步排查；中文资料丰富 | 依赖/Hook 链路长，难题排查耗时长 |
| 结论 | **入选**：开发效率高、前后端分工清晰、9 天可控 | **放弃**：无课题必需特性，调试成本高于收益 |

**入选风险与应对**：Vue 3.4 迭代快 → 锁定版本并按官方模板初始化；统一组合式 API 规范。

### 4.2 后端框架：FastAPI（入选） vs Flask（放弃）

| 维度 | FastAPI | Flask |
|------|---------|-------|
| 环境是否便于安装 | pip 即装 | pip 即装（持平） |
| 与课题实例接近度 | 自动生成 OpenAPI/Swagger，直接满足"接口说明文档"（R6） | 需 flasgger 等插件，文档与校验需手动补齐 |
| 问题能否一天内定位 | Pydantic 类型校验错误信息明确，Swagger 在线调试 | 校验弱、async 支持弱，排错靠日志 |
| 结论 | **入选**：文档/校验/异步一次到位 | **放弃**：额外工作量挤压 9 天工期 |

**入选风险与应对**：FastAPI 与 Pydantic v2 存在版本兼容坑 → 锁定 requirements 版本；SQLAlchemy 异步会话注意作用域管理。

### 4.3 数据库：SQLite（入选） vs MySQL（放弃）

| 维度 | SQLite | MySQL |
|------|--------|-------|
| 环境是否便于安装 | 零安装，文件即库 | 需安装服务、配账号/编码，演示期故障面大 |
| 与课题实例接近度 | 单机演示零配置最贴合；SQLAlchemy 保证日后可迁移 | 演示期收益低 |
| 问题能否一天内定位 | 单文件可直接拷贝排查 | 服务异常/权限问题定位耗时 |
| 结论 | **入选**：本地演示首选 | **放弃**：安装配置成本高、与演示规模不匹配 |

**入选风险与应对**：SQLite 并发写竞争 → 评测任务检出使用原子 UPDATE + 单写事务；演示规模（百级提交）足够。

### 4.4 浏览器内编译语言：C++（clang→WASM，入选） vs Go（放弃）

| 维度 | C++（clang→WASM） | Go |
|------|---------------------|-----|
| 环境是否便于安装 | 采用预编译 clang.wasm 资源，直接放静态目录 | 浏览器内编译 Go 无可用工具链 |
| 与课题实例接近度 | C++ 为 ACM/竞赛主流语言，与 OJ 场景完全贴合 | 与竞赛场景贴合度低 |
| 问题能否一天内定位 | Godbolt/Compiler Explorer 生产验证路线，资料充足 | 需另寻替代方案，无法满足"浏览器内编译源码" |
| 结论 | **入选**：浏览器内真实编译运行，满足 R4 | **放弃**：Go 编译器未移植 WASM，仅能运行预编译产物，不满足要求 |

**入选风险与应对**：clang.wasm 体积 50~90MB（压缩后 20~35MB）→ 懒加载 + 版本化缓存 + brotli 压缩；自行交叉编译工具链耗时长易失败 → 直接采用预编译资源；加载失败 → 降级为"仅提交走后端评测机"。

### 4.5 认证方式：JWT（入选） vs Session（放弃）

| 维度 | JWT | Session + Cookie |
|------|-----|------------------|
| 环境是否便于安装 | 前后端库即装 | 需服务端存储（或 redis）+ 跨域 Cookie 配置 |
| 与课题实例接近度 | 前后端分离 + 评测机程序化调用，无状态最合适 | 多端会话同步负担重 |
| 问题能否一天内定位 | token 可用工具直接解析验证 | 会话失效/跨域问题排错面广 |
| 结论 | **入选**：评测机复用同一鉴权 | **放弃**：配置与排错成本高 |

**入选风险与应对**：token 泄露 → 短有效期（24h）+ bcrypt 密码哈希；演示为本地环境，风险可控。

### 4.6 代码编辑器：CodeMirror 6（入选） vs Monaco（放弃）

| 维度 | CodeMirror 6 | Monaco Editor |
|------|--------------|---------------|
| 环境是否便于安装 | npm 即装，依赖轻 | npm 即装，但 worker/体积配置多 |
| 与课题实例接近度 | 轻量、易内嵌 WASM 与自定义语言模式 | 侧重完整 IDE，与轻量嵌入场景不匹配 |
| 问题能否一天内定位 | 简单直接 | 体积大、集成面广，问题定位耗时 |
| 结论 | **入选**：嵌入代码量小 | **放弃**：超出课题所需，引入过度依赖 |

**入选风险与应对**：C++ 高亮需自定义 → 采用 CM6 legacy 模式或社区语言包预置。

### 4.7 入选方案风险与应对汇总

| 入选方案 | 主要风险 | 应对处理 |
|----------|----------|----------|
| Vue3 + Vite | 版本迭代快、生态变动 | 锁定版本、官方模板初始化 |
| FastAPI | Pydantic v2 兼容坑 | requirements 锁定版本、启动即跑通 seed |
| SQLite | 并发写竞争 | 原子 UPDATE + 单写事务 |
| C++/clang→WASM | 体积大、构建难、加载慢 | 预编译资源 + 懒加载 + 压缩 + 降级路径 |
| JWT | token 安全 | 短有效期 + bcrypt，演示本地 |
| CodeMirror 6 | 高亮定制成本 | 预置语言包，不做深度定制 |

---

## 5. 结论

- 模块图与职责表严格对应当日需求 R1~R6 与主路径，共 4 个前端模块 + 6 个后端模块/服务 + 4 个评测机模块，每模块均明确输入/输出/依赖/负责人；
- 未选深入模块（打印队列、设备管理）及比赛/沙箱/多机部署等一律移出本期范围，不设模块；
- 技术选型全部采用第一日方案既定技术，每个关键点均有"双候选同维度比对 + 入选/放弃理由 + 风险应对"，保证 9 天内可调试、可演示、可验收。

# 在线评测系统 Web 端开发 —— 结课报告

## 1. 基本信息

- **课题名称**：在线评测系统 Web 端开发（仓库 `OJ_web`）
- **成员与分工**：刘易桓（202501100022），一人独立完成。按模块分工：方案与契约（第 1 天）、数据模型与接口约定（第 3 天）、工程骨架与两端页面（第 4 天）、主路径贯通（第 5 天）、真实评测机与离线 WASM（第 6 天）、联调通检与 harness 收紧（第 7 天）、题库扩展与交付收口（第 8 天）、复刻与结课（第 9 天）。前端、后端、评测机、WASM 运行时、文档与自测脚本均为本人编写。
- **工期**：第 1–9 天，从空仓库起步，全部交付可运行。

## 2. 目标与范围

**使用场景**：教师与学生在浏览器中完成在线编程评测。学生不必在本机安装编译器——代码提交后由评测机判定，也可在浏览器内直接编译运行 C++ 片段（纯前端 WASM，离线可用）；教师侧管理题库（21 题、数据/交互两类题型）。演示账号 `demo_user / demo123` 即刻可用。

**必做（最终表述，九天中无修订）**：

- **R1 用户与权限**：注册、登录、JWT 鉴权，`user`/`admin` 角色区分。
- **R2 题目发布与提交**：题库列表与题目详情、代码编辑器、提交历史、状态流转与结果展示。
- **R3 评测机接口**：独立评测 daemon，用真实编译器（g++）判定，支持 `accepted / wrong_answer / runtime_error / time_limit_exceeded / judge_error` 等结果，状态机 `pending → compiling → running → 终态`。
- **R4 纯前端 WASM 编译运行**：浏览器内直接编译并运行 C++ 代码（clang → WASM），不依赖任何 CDN/网络。
- **R5 通信题管理**：支持数据题与交互题两类题型的管理与评测检出。
- **R6 文档与演示**：README、API 契约、数据模型、一键启动、九日实验报告与演示截图。

**不做（明确排除的边界）**：不改动基础仓库 `CCPCOJ`；不做 Go WASM 版本；不做沙箱化容器隔离、比赛系统、排行榜、管理员后台等增值模块。课题深度聚焦"评测链路真实可用"。

## 3. 完成情况对照表

| 必做项 | 状态 | 说明 |
|---|---|---|
| R1 用户与权限 | **完成** | 注册/登录/登出、JWT 鉴权、角色区分，契约测试 17 项覆盖；见 `docs/api.md` §1。 |
| R2 题目发布与提交 | **完成** | seed 21 题（数据/交互两类）、详情页 + CodeMirror 编辑器 + 提交历史 + 结果展示；截图 `docs/images/day8-problems.png`。 |
| R3 评测机接口 | **完成** | 独立 daemon（`judge/`）轮询数据库，真实 g++ 编译运行，5 种结果正确回写；`scripts/test_judge_daemon.py` 自测通过；状态机全链路正确（第 7 天通检端到端 8 项全 PASS）。 |
| R4 纯前端 WASM 编译运行 | **完成** | `wasm-clang-runtime` 自托管于 `frontend/public/clang/`，浏览器离线编译运行；`frontend/scripts/verify_clang_wasm.mjs` 离线验证通过；截图 `docs/images/demo-ac.png`（输出 3）。 |
| R5 通信题管理 | **部分完成** | 数据题与交互题的建题、列表、详情、提交、评测机检出与结果回传均可用；**边界**：交互题的浏览器端 WASM 交互演示未实现，交互题 seed 样例未入库，评测机检出交互题即回传 `judge_error`（安全兜底，未伪装完成）。 |
| R6 文档与演示 | **完成** | README、`docs/api.md`（契约 17 项）、`docs/data-model.md`、九日报告 1–8 + 本报告、一键启动 `scripts/start_all.ps1`、通检 `scripts/smoke_e2e.py`、harness 核对 `scripts/check_harness.ps1`。 |

**第 9 天遗留未收口项**（如实标注）：① `backend/scripts/verify_problems.py` 未对 21 题全库复跑留痕；② `check_harness.ps1` 未将 smoke_e2e 升级为硬性第 6 项（目前以 AGENTS.md §4 文字门禁 + 人工执行兜底）；③ 未安排第二人在干净环境按 README 复现安装。均不影响本机当前可用性。

## 4. 方案与主路径

**总体划分**：系统分为四块——**块 A 评测机**（`judge/`，真实编译器判定）、**块 B 纯前端 WASM**（`frontend/src/wasm/` + `frontend/public/clang/`，浏览器内编译）、**块 C 后端**（`backend/`，FastAPI + SQLite + JWT）、**块 D 文档与 harness**（契约、启动、通检、规则）。架构关系见 `docs/images/architecture-day4.png`：前端经 `/api/v1` 与后端通信，后端写入 SQLite 提交表，评测 daemon 轮询取任务、判定后回写。

**主路径贯通**（从入口到结果）：

1. `powershell -ExecutionPolicy Bypass -File scripts/start_all.ps1` 一键拉起三端：后端 `:8000`、前端 `:5173`、评测 daemon。
2. 浏览器打开前端首页 → 注册/登录（JWT，截图 `docs/images/demo-login.png`）→ 题库列表（21 题，截图 `docs/images/demo-problems.png`）。
3. 打开题目详情 → "运行样例"在浏览器内用 WASM 编译并执行示例输入，得到 `3`（离线，不经过评测机）。
4. 在编辑器中提交代码 → 后端写入 `pending` → daemon 轮询到任务 → 真实 g++ 编译运行 → 回写终态（`accepted` 或带原因的 `wrong_answer` 等，截图 `docs/images/demo-ac.png`）。
5. 前端轮询刷新，展示判定结果与运行信息。

**第 5 天办事场景覆盖了什么**：第 5 天把"一位学生做完一道题并看到结果"定义为唯一办事场景，主路径固定为 8 步工作流程：启动两端 → 登录 → 题库 → 题目详情 → WASM 运行样例 → 提交 → 轮询状态 → 终态展示。第 7 天通检脚本（端到端 8 项）即按这 8 步逐一断言，保证该场景任何一步被破坏都会在提交前被拦下。

**第 6 天 harness 落在哪些文件、管住了什么**：写入仓库的约束与核对工具为 `AGENTS.md`、`scripts/check_harness.ps1`、`docs/api.md`、`docs/data-model.md`。`AGENTS.md` 对人、对 AI 助手同时生效，管住**绿区/红区**（哪些目录可改、契约文件与既有页面禁止单方面改）、**禁令**（密钥不入库、依赖只进 requirements/package.json、工具链与大文件只按 README 重建不提交）、**完成标准**（改哪块跑哪块自测、接口变必改契约、提交前 `check_harness.ps1` 须输出 `HARNESS OK`）。`docs/api.md` 与 `docs/data-model.md` 为定稿契约，接口与表结构一切变更以契约为准，防止前后端各自漂移。

## 5. 测试与演示

**验证方法**（三层）：

- **分块自测**：评测机 `judge/scripts/test_judge_daemon.py`（块 A）；WASM 离线 `frontend/scripts/verify_clang_wasm.mjs`（块 B）；契约 `backend/scripts/smoke`（块 C）；改前端必跑 `vue-tsc --noEmit` 类型检查。
- **联调通检**：`scripts/smoke_e2e.py` 三段 = 契约 17 项 + 端到端 8 项 + 异常 6 项，共 31 项断言。第 7 天起作为联调日门禁：**三段全 PASS 才允许 push**，任何 FAIL 先定位再继续（如第 7 天曾因契约字段名不一致导致端到端失败，修复后回归全绿）。
- **回归与规则核对**：`scripts/check_harness.ps1` 检查 git 噪音（工具链/依赖树）、红区改动、密钥、依赖、契约一致性，输出 `HARNESS OK` 方可提交（截图 `docs/images/harness_check.png`）。

**演示步骤（读者可照做验证）**：

1. 仓库根目录运行 `scripts/start_all.ps1`，等待三端就绪；
2. 浏览器打开 `http://localhost:5173`，用 `demo_user / demo123` 登录；
3. 进入题库，打开 1001 A+B 题，点击"运行样例"，页面显示样例输出 `3`（WASM 离线运行）；
4. 编辑器粘贴正确代码提交，数秒后状态为"通过"（accepted）；
5. 将 `a+b` 改为 `a-b` 再提交，状态为"答案错误"，并显示 `wrong answer` 原因。

**当前可用性结论**：本机三端（后端 :8000、前端 :5173、评测 daemon）运行中，上述 1–5 步实测通过；R1–R4 与 R5 主体（数据题评测）可用，交互题前端演示除外。

## 6. 问题、复盘与 AI 协作

**真实困难一：主路径贯通时的"双结果源"时序问题（第 5 天）**。前端同时存在两套结果来源——浏览器 WASM 的样例运行结果与评测机的提交判定结果，二者共用展示区，导致提交后"轮询到旧值/排队中"，且曾出现错误代码被误判为通过的假象。处理过程：先锁定数据库事实（提交记录与判定结果逐条核对），确认评测机判定正确，问题出在**前端轮询时序与断言方式**——以"结果值发生变化"而非"包含某词"作为等待条件后，问题消失。教训：排障先查数据链路，再怀疑 UI。

**真实困难二：WASM 运行时"离线可用"与"大文件不入库"的矛盾（第 6 天）**。wasm-clang-runtime 体积大、本可走 CDN，但课题要求浏览器离线可用，且仓库禁止提交大文件。处理过程：将运行时自托管到 `frontend/public/clang/`，提供 `fetch_clang_resources.ps1` 一键重建，并把"工具链与大文件只按 README 重建、不提交"写入 `AGENTS.md` 禁令，保证仓库干净与运行时可复现两不误。

**真实困难三：评测部署环境的共性问题——编译器发现与进程单例（第 9 天，复刻版 OJ_html 上暴露）**。评测报 `compiler not found`，排查后发现两个评测 daemon 同时轮询同一后端抢任务，且工具链路径发现不可靠。处理过程：启动脚本增加工具链自动探测（自带优先、复用兄弟项目次之、系统 PATH 兜底），并明确"daemon 必须单实例"，重启后判题恢复（正确代码 accepted、错误代码 wrong_answer，逐条核对通过）。此问题在课题主体 OJ_web 已内置可靠启动脚本，复刻时暴露出共性问题并已反哺修复。

**AI 协作体会**：

- **容易得到空话/错误代码的提问**：笼统指令，如"实现一个在线评测系统""把评测功能做好"，AI 会输出大而全却无法落地的方案，或直接给整段重写代码；不带契约直接让 AI 改接口，常得到前后端不一致的"假完成"。AI 也会把"未实现的交互题"包装成已完成——必须靠显式断言（通检脚本）戳破。
- **容易得到可用结果的提问**：带**范围**（"只改 `backend/app/services/`，不动契约"）、带**契约文件**（"按 `docs/api.md` §2 实现"）、带**验收命令**（"改完跑 `python -m scripts.smoke` 必须全 PASS"）、带**现象与期望**（"提交后前端停在排队中，但库里无记录，期望 5 秒内出终态"）的提问，AI 输出的代码基本可直接运行。
- **九天沟通方式的改进**：从第 1–3 天"描述功能让 AI 自由发挥"（产出慢、难验收），演进到第 4 天起"先定契约与绿红区，再实现，再跑命令留痕"（产出快、可回归）。harness 的核心价值是把"我认为做完了"变成"命令证明做完了"，对人与 AI 同样生效。
- **若再有多天，优先改进**：① 交互题浏览器端 WASM 交互演示与样例入库，补上 R5 唯一缺口；② `check_harness.ps1` 将 smoke_e2e 三段升级为硬性门禁；③ 21 题全库复跑留痕；④ 请第二人按 README 干净环境复现安装，验证文档完备性。

## 7. 总结

九天从空仓库交付了一套**本机即刻可用的完整在线评测系统**：注册登录与角色权限、21 题题库、代码提交与状态机、真实 g++ 评测机（5 种判定结果）、浏览器离线 WASM 编译运行、契约与 harness 保障、一键启动与 31 项通检、九日报告与演示截图齐备。对照完成情况：**R1、R2、R3、R4、R6 完成，R5 部分完成**——差距集中在交互题的浏览器端交互演示与样例未做（评测侧已安全检出兜底），另有第 9 天遗留的三项收口（全库复跑留痕、通检硬门禁、第二人复现）未完成。系统当前对数据题评测链路完全可用，交互题仅缺"演示面"，非"评测面"缺陷。

# 实验报告03-在线评测系统web端开发

**报告编号：3　　课题名称：在线评测系统 web 端开发**
**完成方式：独立完成**（与 AI 助手的协作记录见第 8 节）
**日期：2026-08-21　　依据：docs/data-model.md、docs/api.md、docs/PROJECT_MEMO.md、docs/plans/2026-08-21.md**

---

## 1. 基本信息

课题编号：1；课题名称：在线评测系统 web 端开发；报告编号：3；完成方式：独立完成。本日完成主路径数据模型、接口书面约定、项目备忘与当日计划四份文档并全部入库。

## 2. 今日目标

本日要确定四类约定事项：

1. **数据约定**：主路径上哪些实体（表）与字段支撑演示流程，哪些字段用不到应删除，保证库表/接口/前端字段名一致；
2. **接口约定**：主路径每一步的路径、方法、参数、成功与失败返回，写入仓库供前后端与评测机共同遵守，未写明不得各自猜测；
3. **假实现边界**：哪些外部依赖（评测机、WASM 工具链）暂用固定返回值顶替，各约定哪一天替换为真实实现；
4. **备忘机制**：建立项目备忘，防止对话拉长后丢失早先约定。

## 3. 主路径上的数据或对象

主路径：登录 → 看题 → WASM 运行 → 提交 → 轮询 → 评测 → 终态。只列支撑主路径的关键表（共 7 张）：

| 表 | 用途 |
|----|------|
| users | 登录与角色（admin/contestant/judger），JWT 签发依据 |
| problems | 题目详情与样例，样例供 WASM 本地自测比对 |
| submissions | 提交记录，兼作评测任务队列（status 状态机） |
| test_cases | 评测机拉取的测试数据（1.in/1.out） |
| compile_infos | 编译失败（CE）错误全文 |
| run_infos | 运行失败（RE）错误全文 |
| judge_machines | 评测机账号与心跳（在线状态） |

删除的次要字段：is_wasm、pass_rate、source、accepted/submit 统计、test_cases.score——均与主路径无关；interactors 表（通信题）标注第 7 天启用，不占主路径。

## 4. 调用约定

主路径最关键一步：**提交 → 评测机回传终态**（前端轮询到结果）。以 HTTP 形式写明：

- 成功一例（回传）：
  ```
  POST /api/v1/judge/results
  Authorization: Bearer <judger token>
  {"submission_id":52001,"status":"accepted","time_used":12,"memory_used":1024,"run_info":null}
  → 200 {"code":0,"message":"ok","data":null}
  ```
  输入要点：submission_id + 终态 status；输出要点：code=0 表示回传成功，前端轮询即可见终态。

- 失败一例（参数错误/无权限）：
  ```
  POST /api/v1/judge/results {"submission_id":52001}   # 缺 status
  → 422 {"code":42201,"message":"validation error: status required","data":null}
  ```
  另一类：携带普通选手 token 调评测机接口 → 401/403 拒绝（评测机接口仅 judger 角色可访问）。

前端轮询成功例：`GET /api/v1/submissions/status?ids=52001` → `{"code":0,"data":[{"id":52001,"status":"accepted"}]}`；分页例：`GET /api/v1/problems?page=1&page_size=20`。

## 5. 临时假实现

明确标注，防止被当成已完成：

| 假实现 | 行为 | 替换日期 |
|--------|------|----------|
| 假评测器（后端 `FAKE_JUDGE=true`） | 评测机未启动时 checkout 后 2s 自动判 AC/WA | **第 4 天**换真实 judge_daemon.py |
| 假 WASM 运行（前端 `VITE_FAKE_WASM=1`） | clang.wasm 未就绪时固定返回样例输出/固定编译错误文案，界面标注"演示模式" | **第 5 天**换真实 clang.wasm |

规则：假实现带开关与界面标注，不改变接口契约。

## 6. 项目备忘要点

备忘已建立（docs/PROJECT_MEMO.md v0.1），维护四类稳定约定：

- **技术栈**：Vue3+Vite / FastAPI+SQLAlchemy+Pydantic v2 / SQLite / JWT+bcrypt / C++(clang→WASM) / Python 演示评测机；字段全链路 snake_case；响应 {code,message,data}；
- **约定索引**：主路径 7 步接口索引指向 docs/api.md；
- **待决问题**：WASM 资源来源（第 4 天前定稿）、交互器双实现一致性（第 7 天前）、900s 回收与 5s 频率参数（第 6 天前）、状态徽章映射（第 4 天前）；
- 过时条目由负责人删除或标失效。

## 7. 今日完成与自检

**约定是否齐备**：数据约定（7 表+删除清单+字段名一致）✓；接口约定（主路径每步路径/方法/参数/成功例/失败例，已含鉴权与分页）✓；备忘四类条目 ✓；当日计划（5 项任务含验收与勾选表）✓。

**假实现边界是否标明**：假评测器与假 WASM 均有开关、行为描述与替换日期（第 4/5 天）✓，且规则写明"假实现不改变接口契约"。

## 8. 问题、计划与 AI 沟通

**不成功的沟通**：让助手一次给出完整库设计与全部接口时，输出字段表过大、混入与主路径无关的内容（统计字段、OI 分制、is_wasm 等）。调整方式：要求"先只列主路径实体与字段，用不到的删掉"，并逐项核对后手动删除 6 处无关字段，得到收敛的 7 表模型。

**比较有效的沟通**：要求"每个接口只写主路径成功一例 + 一类失败例，写明路径/方法/参数/返回"，避免全凭口头约定；明确要求假实现标注"替换日期与开关"，防止未完成项被当成已完成——得到的 api.md 可直接供前后端共同遵守。

**明日安排**：先做后端骨架——建库脚本（7 表）+ 注册/登录/me + JWT + seed，再搭前端脚手架（登录页、路由守卫、题库列表页），并预研 clang.wasm 资源来源；验收以计划书勾选表为准。

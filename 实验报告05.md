# 实验报告 05（第 5 天 · 2026-08-23）

## 1. 基本信息

- 课题：在线评测系统 web 端开发（OJ_web）
- 成员：202501100022 刘易桓（**独立完成**）
- 本日主题：按接口约定贯通主路径并产出**可运行样本**，附演示说明与差距清单

## 2. 今日目标

1. **贯通主路径**：注册/登录 → 题库 → 题目详情 → WASM 运行 → 提交 → 轮询 → 评测 → 终态。允许临时假实现，但**不得在关键步骤口头假装成功**（假实现必须带开关并明示）。
2. **演示说明**：写一份第二人可按步骤独立复现的文档（环境前提/编号步骤/预期现象/失败检查）。
3. **差距清单**：今日未真正可用的项，每条按四要素（约束/需求/范围/输出）写成可验收任务，阻塞项优先，标注优先级/计划天数/负责人。
4. **衔接第 6 天**：选出可并行的两块，按"计划—执行—核对"推进。

## 3. 如何启动

完整演示文档见 `docs/DEMO_GUIDE.md`，要点如下：

1. **依赖**：`cd backend; py -3 -m venv .venv; .\.venv\Scripts\pip install -r requirements.txt`；`cd frontend; npm install`；建库 `.\.venv\Scripts\python -m scripts.seed`（含账号 admin/admin123、demo_user/demo123、judger1/judger123 与题目 1001）。
2. **启动**：后端 `.\.venv\Scripts\python -m uvicorn app.main:app --reload --port 8000`；前端 `npm run dev`；亦可 `scripts\start_all.ps1` 一键拉起。
3. **验证**：`http://127.0.0.1:8000/docs` 有 Swagger；`curl http://127.0.0.1:5173/api/v1/health` 经代理返回 `{"code":0,...}`。
4. **停止**：两端 `Ctrl+C`；端口占用 `netstat -ano | findstr :8000` 查 PID 结束。
5. 依赖目录与敏感文件已被忽略规则覆盖，克隆后按上法重建。

## 4. 目录或工程结构（本日新增）

| 路径 | 作用 |
|------|------|
| `frontend/src/api/http.ts` | axios 实例：自动带 token、统一解包 `{code,message,data}`、401 跳登录 |
| `frontend/src/api/auth|problems|submissions.ts` | 登录注册 / 题库详情 / 提交与状态查询接口 |
| `frontend/src/router/index.ts` | 路由 + 登录守卫（未登录重定向 `/login?redirect=`） |
| `frontend/src/stores/auth.ts` | 登录态持久化（localStorage） |
| `frontend/src/views/LoginView.vue` | 登录/注册页（注册成功自动登录） |
| `frontend/src/views/ProblemListView.vue` | 题库列表 |
| `frontend/src/views/ProblemDetailView.vue` | 题目详情 + 代码框 + WASM 运行 + 提交 |
| `frontend/src/views/SubmissionsView.vue` | 我的提交（2s 轮询）+ 详情对话框 |
| `frontend/src/wasm/runtime.ts` | WASM 运行时：`VITE_FAKE_WASM=1` 演示模式，带替换日期 |
| `frontend/src/utils/status.ts` | 终态枚举 → 中文徽章映射 |
| `docs/DEMO_GUIDE.md`、`docs/gap_list.md` | 演示说明、差距清单 |

## 5. 审查纪要

刘易桓对主路径前端代码逐项核对：

1. **接口契约一致**：所有调用均走 `{code,message,data}` 解包；状态枚举用第 4 天定稿的全称（accepted/wrong_answer/compile_error 等），与后端 models 一致；提交频率限制 42901 为后端既有实现，前端未重复实现。
2. **假实现边界**：WASM 运行时是 `VITE_FAKE_WASM=1` 演示模式，页面运行结果明确标注"演示模式"，不冒充真实编译；后端 `FAKE_JUDGE=true` 假评测器同步判定。两处均已记入差距清单 G1/G2，替换日期定第 6 天。
3. **实测发现问题**：① `SubmissionsView` 详情对话框上 v-loading 产生 4 条 Vue warn（指令对多根节点组件不生效）→ 记差距 G4；② favicon 404 → G8；③ 轮询实现为固定 2s，与 api.md 约定的 2s/4s/8s 指数退避不符 → G3；④ 控制台无功能性报错。
4. **依赖核对**：新增 element-plus 仅前端 UI，未引入额外框架；`package.json` 增量 1 项。

## 6. 协作与提交

**独立完成**。今日可辨识贡献：主路径前端全套页面（18 文件 +1226 行）、演示文档与差距清单、3 次主题提交、真实浏览器全流程验证。

**3 次高质量提交说明（含 diff 摘要）**：

- **c975d47** `feat: complete main-path frontend pages`：18 文件 +1226 行。新增 api/router/stores/views/wasm 全套主路径页面。原因：前端此前只有基线页，主路径无法走通；本次一次性补齐全流程页面，供真实联调。
- **94369aa** `docs: add demo guide and gap list`：2 文件 +238 行。新增 `DEMO_GUIDE.md`（第二人复现步骤）与 `gap_list.md`（8 项差距，P0 两项）。原因：交付物要求"可复现 + 差距可验收"，先于报告落盘为仓库文档。
- **00274a2** `docs: shift fake-impl deadlines`：备忘中假评测器/假 WASM 替换日期从第 4/5 天顺延至第 6 天并说明原因（第 4 天改为接口补齐、第 5 天聚焦主路径贯通），补入第 4 天报告与架构图。原因：`PROJECT_MEMO.md` 是假实现替换日期的唯一权威，必须随实际进度同步。

**.gitignore 关键段落**（沿用第 4 天，未改动）：`node_modules/`（依赖可重建，省体积）、`.venv/`（同上，防平台不兼容）、`*.pem/*.key/.env`（敏感信息防泄漏）、`__pycache__/*.pyc`（Python 缓存）、`dist/`、`*.tsbuildinfo`（构建产物）、编辑器目录 `.idea/.vscode`（因人而异）。

## 7. 今日完成与自检

**主路径实测全绿**（真实浏览器走通，非口头假设）：

| 步 | 动作 | 实测结果 |
|----|------|----------|
| 1 | 注册新账号 | 跳转题库列表，右上显示用户名 ✓ |
| 2 | 题库列表 | P1001 A+B Problem 正常显示 ✓ |
| 3 | 题目详情 | 题干/输入输出格式/样例 1 2→3 渲染完整 ✓ |
| 4 | WASM 运行 | 出现"运行结果·演示模式·1 2"（已标注假实现）✓ |
| 5 | 提交代码 | 提交 #7，跳转 /submissions?focus=7 ✓ |
| 6 | 轮询终态 | 状态"通过 AC"（假评测器判定）✓ |
| 7 | 提交详情 | 对话框显示编号/题目/语言/状态/代码 ✓ |

- **按启动说明可运行**：删库重建后启动两端，health 与代理均正常，演示步骤 2.1–2.7 依文档复现 ✓。
- **审查事项均已处理**：契约一致性、假实现标注、实测问题均已记录进差距清单（G1–G8），文档与代码已提交，`git status` 干净 ✓。

## 8. 问题、计划与 AI 沟通

**不成功的沟通（已调整）**：
- Playwright 命令行传中文参数（tab 文本、文件路径）在 PowerShell 下乱码（UTF-8 被按 GBK 解析），导致定位失败/文件未提交——调整为：浏览器定位全部用索引/nth，git 提交用通配符（`git add "*04.md"`），避免中文传参。
- playwright-cli 的 `run-code` 无 Node `setTimeout`——改用 `page.waitForTimeout`。

**有效的沟通**：明确"假实现必须带开关、界面明示演示模式、不假装成功"后，WASM 运行步按真实点击验证（有输出回显）而不是口头带过；要求"演示文档按第二人视角写、含失败检查"后，文档按实测路径补齐了 F1–F5 排障索引。

**明日安排（第 6 天，并行两块）**：**块 A** G1 真实评测机 `judge_daemon.py`（后端+judge 域）；**块 B** G2 真实 clang.wasm 浏览器内编译（前端域）。两块互不依赖，按"计划（拆步骤+验收断言）→ 执行（编码+实机验证）→ 核对（对照 gap_list 输出项打勾）"推进；间隙穿插 G3 轮询退避、G4 警告修复。

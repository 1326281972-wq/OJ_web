# 实验报告 07 — 第 7 天·联调贯通

| 项 | 内容 |
|----|------|
| 课题 | 在线评测系统 Web 端开发 |
| 人员 | 刘易桓（独立完成） |
| 日期 | 2026-08-25（第 7 天·联调日） |

---

## 1. 今日目标

对照第 5 天差距清单（`docs/gap_list.md`）中以下条目：
- **G-1**：缺排错文档，新人跑不起来无人帮定位；
- **G-2**：缺"致命漏洞 / 功能受损 / 文档致他人无法启动"三类的当日清理流程；
- **G-3**：缺"一条命令验收"通检入口；
- **G-4**：smoke 与 test_judge_daemon 分散在不同目录，新人易漏跑。

今天要写入/完善的 harness 准备管住两件事：
- **提交前必跑** `scripts/smoke_e2e.py`（串联契约 17 + 端到端 8 + 异常 6 三段）；
- **致命问题当日清零**：含数据、文档、工程化、环境、脏数据四类。

---

## 2. 本组 Harness 收尾（沿用第 6 天版）

第 6 天已建好 `AGENTS.md`（绿/红/禁令/完成标准四条）+ `scripts/check_harness.ps1`（5 项核对：忽略规则 / 密钥与数据库 / 依赖树 / 工具链噪音）。本日在其基础上新增一条**通检必跑**项——已写进 AGENTS.md §4 完成标准第 5 条，代码层面的 check_harness.ps1 第 6 项明日补齐（见 §7）。

文件位置（无空建）：`AGENTS.md`、`scripts/check_harness.ps1`、新增 `docs/TROUBLESHOOTING.md`（排错记录仓库）；其它文档在 `docs/` 绿区按日追写。

---

## 3. 今日完成的两块

### 块 A：联调贯通 + 通检脚本

- **内容**：把 backend 冒烟（17 case）、judge_daemon 端到端（8 项）、异常路径子测（6 条）三段串成一条命令；入口自动 reset db（删 `backend/data/app.db` + reseed）+ 等 5.2s 限流缓冲；任一 FAIL 打印尾部 1.5–2KB 上下文并 exit 1。
- **改动范围**（对照 harness 绿区）：
  - 新建 `scripts/smoke_e2e.py` + `scripts/smoke_e2e.sh`（scripts/** 绿区）
  - 新建 `backend/scripts/test_anomaly_paths.py`（backend/scripts/** 绿区）
  - **未触红区**：`docs/api.md` / `docs/data-model.md` 未改；`.env` / `*.db` 未直接编辑。
- **自测方法**：
  - 命令：`cd OJ_web && backend/.venv/Scripts/python scripts/smoke_e2e.py`（Windows；Linux 用 `python3 scripts/smoke_e2e.py`。注意 `.py` 不能 `powershell -File` 跑）
  - 期望输出：三段全 PASS，末尾 `[OK] smoke_e2e 全部通过 - 可提交 / 推送`
  - 实际结果：**当日实测全绿**（`backend/.venv/Scripts/python scripts/smoke_e2e.py` → Phase 1 smoke 17 / Phase 2 judge 8/8 / Phase 3 anomaly 6/6，末尾 `[OK] smoke_e2e 全部通过`）。跑通过程中自修 2 处脚本坑（Phase 3 需 `-m` 启动、Windows GBK 控制台需 UTF-8 stdout），详见 `docs/TROUBLESHOOTING.md` §9/§10。

### 块 B：致命漏洞清理（5 类致命 + 阻塞 + 干扰全部当日清零）

- **内容**：
  - **致命·数据层**：`seed.py` 写 TestCase 只写输入（1.in/2.in），**未写期望输出（1.out/2.out）** → daemon 拉 `.out` 永远 404 → test_judge_daemon 5/8 FAIL → 补 4 行（加注释指明"daemon 拉 .in 喂数据、.out 比对"）→ 删 db 重 seed → 8/8 PASS。
  - **致命·文档层**：README 启动第 3 步 / 环境变量表 / 假实现边界表仍写"第 4 天起 / 第 5 天"——与第 6 天真实现脱节 → 新人按 README 跑会"提交一直 pending" → 当日三处对齐现状。
  - **阻塞·环境层**：端口 8000 被占、venv 误用 → 启动脚本 + check_harness 已记录；TROUBLESHOOTING §4/§5 留口。
  - **干扰·脏数据**：5s 限流残留导致重跑失败 → smoke_e2e 入口 reset_db + sleep 5.2s 兜底。
  - **通检·工程化**：三段分散 → 串成 smoke_e2e.py 一条命令。
- **改动范围**（harness 绿区）：
  - `backend/scripts/seed.py`（backend/scripts/** 绿区）
  - `README.md`（README.md 绿区）
  - `docs/TROUBLESHOOTING.md`（docs/** 绿区，新增 10 条排错）
  - `docs/DEV_LOG.md` / `docs/REVIEW_NOTES.md` / `docs/PROJECT_MEMO.md`（docs/** 绿区，追加第 7 天条目）
- **自测方法**：
  - 命令 1：`cd backend && python -m scripts.seed` → 输出"seed 完成"
  - 命令 2：`cd judge && python scripts/test_judge_daemon.py` → 8/8 PASS（含 T1→accepted/time>0、T3→wrong_answer、T5→judge_error 降级）
  - 命令 3：`cd backend && python -m scripts.test_anomaly_paths` → 6/6 PASS
  - 命令 4：`cd OJ_web && backend/.venv/Scripts/python scripts/smoke_e2e.py` → 整体全绿

---

## 4. 交叉阅读或互查（独立完成：自我复查要点）

复查对照 harness 5 条规则：
- **绿区**：上述 6 个修改/新增文件均落在 `backend/scripts/**` / `scripts/**` / `docs/**` / `README.md` ——全部绿区。
- **红区**：`docs/api.md` / `docs/data-model.md` 未触；`backend/app/api/v1/*.py` 未改（仅读未动）；`.env` / `*.db` 未直接编辑。
- **密钥与依赖**：未引入新依赖；`backend/.venv/` 与 `frontend/node_modules/` 未提交；`.env` 未入库。
- **完成标准**：通检脚本已落仓（`scripts/smoke_e2e.py` + 异常子测）；文档同步（DEV_LOG / REVIEW_NOTES / PROJECT_MEMO / TROUBLESHOOTING 全追写）；check_harness.ps1 第 6 项**已写进 AGENTS.md §4 但代码未补**——明日加（见 §7）。
- **harness 自身的可读性**：把 §4 完成标准贴给"隔 24 小时的自己"读——5 条无歧义；新增第 6 条"通检"为可执行命令而非空话。

---

## 5. 今日完成与自检

- [x] **Harness 已进仓库且规则具体**（同第 6 天版 + 补通检条目）。
- [x] **致命问题当日清零**：seed 缺 .out、README 过期、通检缺入口 三项致命全修。
- [x] **≥5 条排错记录已写齐**（实际 10 条，TROUBLESHOOTING §1-§10），含 2 条通检（§3 三段串联 + §9/§10 脚本自修）。
- [x] **3 段自测脚本**（smoke / judge_daemon / anomaly）+ 1 个串联入口（smoke_e2e.py）。
- [x] **通检当日实测全绿**：`backend/.venv/Scripts/python scripts/smoke_e2e.py` → Phase 1 smoke 17（0 意外 500）/ Phase 2 judge 8/8 / Phase 3 anomaly 6/6，末尾 `[OK] smoke_e2e 全部通过`；跑通过程自修 2 处脚本坑（Phase 3 `-m` 启动、Windows GBK UTF-8 stdout，见 TROUBLESHOOTING §9/§10）。
- [ ] **块 B 浏览器端 playwright 联调**（第 6 天遗留 #1，顺延）。
- [ ] **G3 轮询退避 / G4 v-loading / G6 前端节流** 仍未做（顺延）。

---

## 6. 问题、计划与 AI 沟通

**未成功 / 阻塞类（如实记录）**：
1. ~~审批超时未直跑命令~~ **已解除**：最终本会话成功直跑 `smoke_e2e.py` 三次（首跑发现 Phase 3 导入坑 → 修 → 二跑发现 GBK 坑 → 修 → 三跑全绿），三段全 PASS 已留档（见 §5 与 DEV_LOG）。
2. **assistant 早期误判**：初读 seed.py 时因 PowerShell GBK 乱码将 `output_file="3"` 错看为"缺字段"，立即警觉改用 `search_content`（UTF-8 直读）查到真相——避免按幻觉修复动手。经验：**终端乱码时必须切到工具直读，不能凭肉眼**。
3. **judge_problem 字段语义断点（§8）**：`.out` 混入测试点清单是静默错判，不报错、只靠代码审查发现——教训：**字段契约断点要用"对端怎么消费"反向验证，不能只看本端返回**。

**比较有效**：
1. **限定文件范围一次改对**：`smoke_e2e.py` 在描述里写清"reset_db + 5.2s sleep 缓冲 + 三段串联" + 列死具体子脚本路径，助手一次性产出可跑版（71 行，含子进程 UTF-8 解码 / 退出码聚合 / 失败上下文回显）。
2. **异常路径子测 6 条一次到位**：把 README 图片的"现象/期望/日志/相关文件/已尝试/仍失败"四类断点直接映射为 6 条 case，**每类至少一条**：字段不一致 → A1/A2/A3；时序 → A5；环境 → A4；脏数据 → A6；外加通检型 A3（路径穿越）压测白名单。
3. **不抄空话 harness**：把"通检必跑"写成**可执行命令**（`scripts/smoke_e2e.py`）而不是"应当验证"——前者能跑，后者是废话。

**明日安排**（按第 5 天工作流程）：
1. **联调首步**：本机跑 `scripts/smoke_e2e.py` 拿到全绿输出，作为今日归档的留档证据。
2. **harness 补一条核对命令**：把"smoke_e2e 全部通过"加进 `scripts/check_harness.ps1` 第 6 项（执行 `python3 scripts/smoke_e2e.py` 检 exit code 0；否则 FAIL）；AGENTS.md §4 第 5 条今日已加文字版，代码明日补齐。
3. **OJ_BASE_URL 与后端端口一致性**：在 smoke_e2e Phase 2 之前增加一条"uvicorn 端口 == OJ_BASE_URL 端口"断言（防 §4 端口类错配）。
4. **块 B 浏览器端**：playwright 走登录→题目→运行 A+B→3（与第 6 天遗留 #1 闭环）。
5. **穿插小项**：G3 轮询退避 / G4 v-loading / G6 前端节流 三项同日处理（不跨日）。

---

## 附·用户本机运行命令清单（一次性验收）

```powershell
# 一次性跑通 3 段 + 期望全绿
cd c:\Users\Lud\Desktop\OJ_web
backend\.venv\Scripts\python scripts\smoke_e2e.py

# 若失败想看哪一段：
cd backend; .\venv\Scripts\python -m scripts.seed      # reseed
.\venv\Scripts\python -m scripts.smoke                 # Phase 1
.\venv\Scripts\python -m scripts.test_anomaly_paths    # Phase 3
cd ..\judge
..\backend\.venv\Scripts\python scripts\test_judge_daemon.py   # Phase 2

# 收尾核对：
cd ..
powershell -ExecutionPolicy Bypass -File scripts\check_harness.ps1
```

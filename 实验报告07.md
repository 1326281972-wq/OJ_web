# 实验报告 07（第 7 天 · 2026-08-25 · 联调贯通日）

## 1. 基本信息

- 课题：在线评测系统 web 端开发（OJ_web）
- 人员：刘易桓（202501100022），**独立完成**

## 2. 今日目标

把第 6 天两块真实实现（G1 真实评测机 `judge_daemon.py`、G2 真实 WASM 编译）与既有前后端按**第 5 天工作流程**贯通成一条可走通的路径：从"提交 A+B 代码"到"真实编译运行给出 accepted / wrong_answer"不再依赖任何假实现。同时建立**一条命令的快速通检**（至少覆盖一条异常路径），致命问题当日清零。贯通目标线：**提交 → 评测机拉任务 → 真实 g++ 编译 → 逐测试点运行 → 回传终态**。

## 3. 贯通说明

- **串联的模块/脚本**：后端契约 API（`submissions` / `judge`）+ `judge/judge_daemon.py`（真实评测机）+ 种子数据（`seed.py`）+ 冒烟（`smoke.py`）+ 异常路径子测（`test_anomaly_paths.py`），最后串成入口 `scripts/smoke_e2e.py`（Phase 1 契约 17 → Phase 2 端到端 8 → Phase 3 异常 6）。
- **替换的临时假实现**：第 5 天演示中后端假评测器（`FAKE_JUDGE=true` 同步判 AC）第 6 天已替换为 `judge_daemon.py`；今日贯通时发现并修正两处让"真"变不出来的断点（见 §5），并把 `.env.example` 残留的 `FAKE_JUDGE=true` 对齐为 `false`——新成员复制模板不再误入假评测器模式。
- **未替换/未实测部分**：前端真实 WASM 的**浏览器内实测**（playwright 走 登录→运行）与"第二人从零复现"仍留第 8 天（见 §6）。

## 4. 快速通检

主路径条目对应第 5 天工作流程步骤编号（实验报告05 §3：1 启动两端 → 3 登录 → 4 题库 → 5 详情 → 7 提交 → 8 终态）。运行方式：`cd OJ_web && backend/.venv/Scripts/python scripts/smoke_e2e.py`。

| # | 步骤（对应第 5 天流程） | 期望 | 实际 | 通过 |
|---|------------------------|------|------|------|
| T-1 | 启动后端并跑通检入口（步骤 1） | 三段全 PASS，末尾 `[OK] smoke_e2e 全部通过` | 三段全绿 | ✅ |
| T-2 | 提交 A+B 正确解（步骤 7→8） | 真实评测返回 `accepted` 且耗时 > 0 | T1 通过（真实编译 30ms 级） | ✅ |
| T-3 | 提交语法错误代码（步骤 7 异常） | 返回 `compile_error` 且带诊断 | T2 通过 | ✅ |
| T-4 | 提交错解（步骤 7 异常） | 返回 `wrong_answer` | T3 通过 | ✅ |
| T-5 | 题目不存在（异常路径 A1） | 明确 404/错误响应，不 500 | 通过 | ✅ |
| T-6 | 非法 status 注入（异常路径 A2） | 拒绝非法值，不崩 | 通过 | ✅ |
| T-7 | 路径穿越尝试（异常路径 A3） | 被白名单拦截 | 通过 | ✅ |
| T-8 | 评测机回传 system_error（异常路径 A6） | 状态可查、可降级不阻塞 | 通过 | ✅ |
| T-9 | 契约冒烟 17 case（步骤 1 环境） | 0 个意外 500，24 个预期码命中 | 全绿 | ✅ |

共 9 条，其中 **T-3 ~ T-8 为异常/失败路径（6 条）**，满足"≥5 条且含 ≥1 异常路径"。

## 5. 问题与处理

**问题 1（致命·数据层）**：`seed.py` 写测试点只写 `1.in/2.in`，**缺 `1.out/2.out`**。
- 现象：`test_judge_daemon` 5/8 FAIL，日志 `GET /api/v1/judge/testdata/1001/1.out → 404 Not Found`，所有提交 `system_error`。
- 期望：daemon 能拉到 `.in`（喂输入）与 `.out`（比对期望）。
- 已尝试：确认 daemon 拉取循环与 `judge.py` 的 testdata 端点逻辑，定位为种子数据缺字段。
- 结论：`seed.py` 补为 4 行（`1.in`+`1.out`+`2.in`+`2.out`），删 db 重 seed 后 **8/8 PASS**。

**问题 2（致命·字段语义）**：`judge_problem` 把 `.out` 文件也列为测试点。
- 现象：`GET /api/v1/judge/problem/{id}` 的 `test_cases` 含 `1.out`，daemon 对每个 name 用 `name.replace(".in",".out")` 拉期望输出，遇 `1.out` 时把**期望输出当输入**喂给程序——静默错判，不报错。
- 期望：清单只列 `.in`（期望输出由 daemon 自行推导）。
- 已尝试：按"对端如何消费"反向核对 daemon 循环。
- 结论：`judge.py` 改为只返回 `.in` 结尾的 case，通检 Phase 2 8/8 复验通过。

**harness 有没有拦住**：第 6 天已写入 harness（`AGENTS.md` 四条 + `scripts/check_harness.ps1` 五项）。本次联调中它**拦住了提交边界类问题**——今日所有改动均落在绿区、无 `.env/*.db` 入库、无依赖树/工具链噪音，提交前 `check_harness.ps1` 输出 `HARNESS OK`；但它**没能拦住数据正确性与字段语义断点**（问题 1/2 不违反任何一条 harness 规则，只能靠通检脚本暴露）。为此收紧：`AGENTS.md` §4 新增第 5 条"联调日必跑通检三段全 PASS，否则禁止 push"（今日已加文字版）；把 `smoke_e2e` 并入 `check_harness.ps1` 第 6 项（提交前自动核对）留第 8 天补代码。

## 6. 遗留问题（第 8 天）

1. 浏览器端 playwright 联调：登录→题目→运行 A+B→3 并截图（第 5 天流程步骤 2~6/8 的真实浏览器证据）——**影响主路径演示完整性，不影响已贯通的后端评测链路**。
2. `check_harness.ps1` 第 6 项并入通检；`OJ_BASE_URL` 与后端端口一致性断言。
3. 小项 G3 轮询退避 / G4 v-loading / G6 前端节流（第 5 天清单顺延项）。
4. 第二人按 `DEMO_GUIDE.md` 从零复现并回填（第 5 天约定）。

以上 1 影响演示完整性，2~4 不阻塞主路径（通检可随时复跑）。

## 7. 今日完成与自检

- **通检通过 9/9**（含 6 条异常/失败路径），实际三段：smoke 17（0 意外 500）、judge_daemon 8/8、anomaly 6/6。
- **致命问题当日清零 2 个**（seed 缺 .out、.out 当测试点）+ 文档/配置 3 处对齐（README、`.env.example`、假实现边界表）。
- **排错记录 10 条**（`docs/TROUBLESHOOTING.md` §1-§10，按"现象/期望/日志/相关文件/已尝试/仍失败/修复/故障层次"模板）。
- 遗留是否写清：见 §6，全部排期第 8 天并标注影响面；`DEV_LOG` / `REVIEW_NOTES` / `PROJECT_MEMO` 已同步。

## 8. 问题、计划与 AI 沟通

**不成功的沟通**：
- 初版通检脚本 Phase 3 用 `python scripts/test_anomaly_paths.py` 直跑报 `ModuleNotFoundError: No module named 'app'`——只报"跑失败"时助手无法定位；补充完整 traceback 后定位为 sys.path 问题（应 `-m scripts.test_anomaly_paths`），一次改对。
- Windows 控制台打印含 `\u0368` 字符时 `UnicodeEncodeError`（GBK 编码）——说明具体报错字符与场景后，加 `sys.stdout.reconfigure(utf-8)` 解决。
- 读 `seed.py` 时终端 GBK 乱码把 `output_file="3"` 错看为"缺字段"——改为工具直读（UTF-8）后看到真相，避免按幻觉改码。

**比较有效的沟通**：
- 按"现象 / 期望 / 已尝试"三段式提问：给出"Phase 2 5/8 FAIL + 日志 `1.out 404`"后，助手定位到种子缺 `.out` 并给出可验证复跑命令（删 db → reseed → 8/8）。
- 要求"字段契约用对端消费方式反向验证"后，暴露出 `judge_problem` 的 `.out` 静默错判——这类断点单端断言查不出来。

**明日安排（第 8 天）**：优先浏览器端 playwright 联调（补第 5 天流程 2~6/8 真实截图与复现回填），随后把通检并入 `check_harness.ps1` 第 6 项，再处理 G3/G4/G6 小项；最后统一回归 `smoke_e2e` 与 `check_harness` 双绿后进入交付文档整理。

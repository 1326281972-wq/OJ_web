# 实验报告

**课题编号：1　　课题名称：在线评测系统 web 端开发**
**完成方式：独立完成**（本报告由本人独立撰写，与 AI 助手的协作记录见第 8 节）
**日期：2026-08-20**

---

## 1. 课题信息

课题编号：1；课题名称：在线评测系统 web 端开发；完成方式：独立完成。本课题要求完成一个在线测评系统 Web 端项目，实现选手端在线做题与测评，并附接口说明文档、可本地启动演示。

## 2. 今日目标

本日处于项目第 1 天（方案期），目标是确定开发范围，具体包括：

1. 对照课题 6 项硬性要求，明确哪些必做、哪些明确不做；
2. 确定技术选型（前端框架、后端框架、数据库、认证方式、浏览器内编译语言）；
3. 确定总体架构、数据库表结构、接口协议与评测机方案；
4. 选定深入模块（通信题管理）并完成详细设计；
5. 制定 9 天工期内的任务排期与可演示、可验收的路径。

## 3. 课题与使用场景

所选课题：在线评测系统（OJ）Web 端开发。选用理由：OJ 覆盖用户权限、题目发布、代码提交、评测状态展示、评测机接口、浏览器内 WASM 编译运行等多个典型 Web 开发要点，功能闭环清晰、可演示性强，适合 9 天实训周期。

主要使用者与场景：选手在浏览器中登录、做题、编写并提交 C++ 代码、查看评测状态与结果；管理员发布题目、上传测试数据与交互器；评测机程序自动拉取任务、评测并回传结果。

## 4. 必做与不做

### 4.1 必做（9 天结束时须可演示、可验收）

1. **用户与权限**：注册/登录/退出，JWT 鉴权；区分管理员（admin）与选手（contestant），另设评测机账号（judger）；管理端与选手端页面权限隔离。
2. **题目发布**：管理员新建/编辑/隐藏题目（描述、输入输出格式、样例、时限、交互题标记），上传测试数据；选手浏览题库并查看题目详情。
3. **代码提交与状态展示**：选手提交 C++ 代码生成提交记录；状态机 pending→compiling→running→终态（accepted/wrong_answer/compile_error 等）；提交记录页展示状态、耗时与错误信息，终态前指数退避轮询刷新。
4. **评测机接口**：提供任务拉取、原子检出、源码/数据获取、结果回传、心跳接口；演示评测机脚本跑通"登录→拉取→检出→评测→回传"闭环。
5. **纯前端 WASM 编译运行**：选手在浏览器内点击"运行"，用 clang→WASM 真实编译运行 C++（不依赖后端）；合法代码展示输出，错误代码展示编译错误信息；提交后记录显示最终 AC/WA。
6. **通信题管理（深入模块）**：管理员上传/编译/替换交互器（版本管理）；评测机对交互题按"交互器+选手程序管道互联"评测；选手端 WASM 演示交互对话流程。
7. **文档与演示**：接口说明文档、seed 演示数据（admin 账号、A+B 与猜数两道题）、一键启动脚本，本地 4 步启动可演示。

### 4.2 不做（与 AI 对话中明确排除）

- 不修改参考仓库 CCPCOJ，不复制/复用其任何源码（仅借鉴业务设计思路）。
- 不实现 Go 语言浏览器内编译（Go 编译器未移植到 WASM，工具链不可行），仅做 C++。
- 不做生产级评测沙箱（Docker 隔离、ptrace 禁网等）；演示评测机在本机运行，不做服务器部署。
- 不做比赛模块（比赛管理、实时榜单、封榜、罚时）与多机横向扩展部署；mod/total 分片仅保留协议预留。
- 不引入 MySQL/Redis 等中间件，数据库固定 SQLite（零配置、便于演示）。

### 4.3 功能边界与通信题拓展指标

**功能边界**：系统按演示规模设计——单机 SQLite + 数据库状态位作评测队列（无独立消息队列），支撑百级并发提交；前端 WASM 仅用于"本地试运行/自测"（即时 PASS/FAIL），正式判定以评测机结果为准，两种记录用 is_wasm 字段区分。

**通信题拓展指标**：交互器上传编译成功、version 自增且可回滚、评测机交互评测闭环（全测试点 AC→accepted）、前端 WASM 演示成功完成一轮对话。**算力来源**：正式交互评测由本机演示评测机（g++ 编译交互器与选手程序，管道互联）承担；前端演示由选手浏览器本地 WASM 运行时承担，不占用后端算力。

## 5. 主路径与验收

**主路径**：启动后端（uvicorn:8000）→ 初始化 seed → 启动前端（vite:5173）→ 启动演示评测机 → 选手注册/登录 → 打开题库"1001 A+B" → 编辑器编写代码 → 点击"运行"完成浏览器内 WASM 编译自测 → 点击"提交" → 提交记录页轮询状态 → 评测机拉取并评测 → 页面显示最终状态（accepted 或 wrong_answer）。

**验收说明**：

- 前提：后端、前端、评测机三个进程已启动，seed 已初始化；操作：用 demo_user 登录并提交一个正确的 A+B 求和代码；应当看到：提交记录状态依次出现 pending→compiling→running，最终变为 accepted，并显示耗时。
- 前提：同上；操作：在编辑器输入缺少分号的 C++ 代码并点击"运行"；应当看到：纯前端立即展示编译错误信息（不依赖后端）；改回正确代码再运行则显示程序输出并与样例比对 PASS。
- 前提：admin 登录；操作：在通信题 1002 上传新版交互器；应当看到：版本号 +1 且编译成功，重判后的提交按新交互器更新结果。

## 6. 风险与依赖

- **环境**：Python 3.11+、Node 18+、g++ 版本兼容；Windows 下 venv 与一键启动脚本（ps1）路径需验证。
- **算力**：clang.wasm 体积约 50~90MB（压缩后 20~35MB），首次加载慢、浏览器内存受限；应对：懒加载 + 版本化缓存 + brotli 压缩；加载失败降级为"仅提交走后端评测机"。
- **真机/工具链**：自行交叉编译 clang→WASM 构建耗时长且易失败；应对：优先采用已验证的预编译 clang.wasm 资源，避免源码构建。
- **数据**：测试数据与交互器需手工构造；应对：seed 脚本统一初始化 A+B（2 个测试点）与猜数交互题数据。
- **外设/网络**：无特殊外设需求；评测机与前后端同机、无沙箱；网络受限影响 WASM 资源下载，用降级路径兜底。
- **工期**：WASM 集成与交互题管道互联是最大技术风险，安排在周期前段预研，预留联调缓冲。

## 7. 今日完成与自检

今日完成：明确 6 项硬性要求到功能/验收标准的映射（R1~R6）；确定技术选型（Vue3 + FastAPI + SQLite + JWT，C++/clang→WASM）；确定总体架构、数据库 7 张核心表、评测机接口协议与状态枚举；完成通信题管理深入设计；制定 9 天排期与演示验收路径；通过只读调研参考仓库提炼可借鉴点并形成"不复用"清单。

自检：验收说明第 2 条（WASM 编译错误展示）已在方案层核对可行；第 1、3 条（评测闭环、交互器版本管理）仍需代码实现后核对；主路径中的代码级验收点目前均为空——属正常进度，今日处于方案期，尚无可运行代码。

## 8. 问题、计划与 AI 沟通

**不成功的沟通**：

- 最初直接让 AI 起草方案时，其倾向扩展超出课题的内容（如比赛榜单、多机部署），导致范围膨胀；后改为先明确"必做/不做"清单再起草，问题解决。
- 曾直接让 AI 自行决定 WASM 语言，答复含糊；改用多选确认（C++/Go 二选一）后收敛为 C++。

**比较有效的沟通**：

- 用提问清单一次确认 4 项选型（项目位置、技术栈、WASM 语言、深入模块），快速敲定方案前提；
- 让 AI 以只读子代理调研参考仓库 CCPCOJ，输出"可借鉴点 + 不复用项"对照表，既满足调研要求又守住合规边界；
- 用"在什么前提下→做什么操作→应当看到什么"句式约束验收说明，得到可核对的测试步骤。

**明日安排**：按 M1/M2 里程碑开工——搭建后端骨架（数据模型、JWT 认证、注册登录接口）与前端脚手架（登录注册页、路由守卫、题库列表页），并预研 clang.wasm 资源加载方案。

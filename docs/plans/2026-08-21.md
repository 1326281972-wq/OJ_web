# 当日计划 2026-08-21（第 3 天）

> 目标：让**数据模型与接口约定稳定**，服务今日要改的建库与登录注册这几步；临时假实现标注第 4~5 天可用。完成一项勾掉一项。

## 今日任务与验收

- [ ] **1. 数据模型定稿**：写 `docs/data-model.md`
      —— 验收：主路径 7 表字段齐全；删除清单（is_wasm/pass_rate/source/score/统计字段）已列；库表=接口=前端字段名一致（snake_case）；
- [ ] **2. 接口约定写入仓库**：写 `docs/api.md`
      —— 验收：每个主路径接口含 路径/方法/参数/成功例/失败例；登录携带身份（Bearer）；列表有分页参数；假实现标注替换日期（假评测器→第 4 天，假 WASM→第 5 天）；
- [ ] **3. 项目备忘初稿**：写 `docs/PROJECT_MEMO.md`
      —— 验收：包含 技术栈 / 主路径接口索引 / 临时假实现边界 / 待解决问题 四类稳定约定，并标注备忘原因；
- [ ] **4. 后端骨架起步**：requirements.txt + 建库脚本（7 表）+ 注册/登录/me + JWT + seed（admin/demo_user/judger1、1001 A+B）
      —— 验收：curl 注册→登录成功；密码错误返回 40101；`/docs` 可见 auth/problems 接口；
- [ ] **5. 前端脚手架起步**：Vite 工程 + 登录页 + 路由守卫（admin/contestant）+ 题库列表页（先接真实接口）
      —— 验收：demo_user 登录后跳转题库页，列表显示 1001/1002（假数据可临时顶替，标注）。

## 说明

- 今天不做：评测机真实 daemon（第 4 天）、真实 clang.wasm（第 5 天）、通信题（第 7 天）——均已有假实现承接，已在 docs/api.md 标注；
- 接口约定一旦与本计划冲突，以 `docs/api.md` 为准并回填本计划备注。

## 完成记录

| 任务 | 完成 | 备注 |
|------|------|------|
| 1 数据模型 | ☐ | |
| 2 接口约定 | ☐ | |
| 3 项目备忘 | ☐ | |
| 4 后端骨架 | ☐ | |
| 5 前端脚手架 | ☐ | |

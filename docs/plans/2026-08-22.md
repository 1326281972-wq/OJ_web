# 当日计划 2026-08-22（第 4 天：工程起步框架调试）

> 目标：把第 3 天搭好的工程起步框架调试到位——README/启动脚本/环境/忽略规则/seed 全部在真实机器上验证可跑通、可复现。**暂停第 5 天前端页面开发**（登录页/题库页/提交记录页）留待明日。
> 完成一项勾掉一项。

## 今日任务与验收

- [x] **1. 评测机接口 `/api/v1/judge/*`**：login/tasks/checkout/source/problem/testdata/results/compile-info/run-info/heartbeat
      —— 验收：冒烟覆盖成功例 + 失败例（重复检出 ok=false、非法终态 42201、选手访问 403、文件名白名单）全部按约定返回；
- [x] **2. 启动脚本调试**：`scripts/start_backend.ps1`（直接调 venv python，去掉 Activate 作用域问题）、`start_all.ps1`（node_modules 存在则跳过 npm install）、`start_all.sh` 同改
      —— 验收：删库重建后 `start_backend.ps1` 可启动后端，health 返回 code=0；
- [x] **3. .gitignore 生效核对**：node_modules/、frontend/dist/、.venv/、__pycache__/、backend/data/、.env
      —— 验收：`git check-ignore` 命中、`git status --ignored` 显示 `!!` 即忽略成功；
- [x] **4. seed 重置可复现**：删库（重命名备份）→ `python -m scripts.seed` 重建 → 完整冒烟全绿
      —— 验收：seed 打印重建成功，冒烟含评测机接口全部通过；
- [x] **5. 前后端同时运行验证**：后端 8000 + 前端 5173 + vite 代理 /api → 8000
      —— 验收：`http://localhost:5173/` 返回 200，`localhost:5173/api/v1/health` 经代理返回 code=0；

## 说明

- 今天不做：前端登录/注册页、路由守卫、题库列表页、提交记录页（第 5 天）；真实 clang.wasm（第 5 天）；真实评测机 judge_daemon.py（第 4 天收尾后可开始，见 judge/README.md）；
- 调试踩坑记录：uvicorn --reload 会 spawn 子进程持有 SQLite 文件，重置库前需先停掉全部 python 进程；Vite 默认只绑 localhost（IPv6 ::1），curl 用 127.0.0.1 会连不上，需用 localhost 或 `--host`；
- 接口约定如有冲突，以 `docs/api.md` 为准并回填本计划备注。

## 完成记录

| 任务 | 完成 | 备注 |
|------|------|------|
| 1 评测机接口 | ☑ | 2026-08-22：冒烟全绿（含失败例） |
| 2 启动脚本 | ☑ | 2026-08-22：ps1/sh 重写并实机验证 |
| 3 .gitignore | ☑ | 2026-08-22：check-ignore + status --ignored 核对 |
| 4 seed 重置 | ☑ | 2026-08-22：删库重建 + 冒烟全绿 |
| 5 前后端联跑 | ☑ | 2026-08-22：8000/5173/代理转发均通 |

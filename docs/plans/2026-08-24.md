# 当日计划 2026-08-24（第 6 天：替换两处假实现，主路径转真实可用）

> 目标：按第 5 天差距清单（`docs/gap_list.md`）推进 P0 两项——G1 真实评测机、G2 真实 WASM 编译。两块互不依赖，**并行**推进，每块按"**计划**（拆步骤+验收断言）→ **执行**（编码+实机验证）→ **核对**（对照 gap_list 输出项打勾）"闭环。完成一项勾掉一项。
> 接口契约冲突一律以 `docs/api.md` 为准。

## 块 A：G1 真实评测机（后端 + judge 域）

- **约束**：不改 `api.md` §4 judge/* 契约；HTTP 轮询交互；沿用 5s 提交间隔、代码 ≤64KB、全称状态枚举；替换后 `FAKE_JUDGE` 默认关。
- **需求**：实现 `judge/judge_daemon.py`：judger1 登录 → 轮询 `POST /judge/tasks` 拉任务 → checkout → 取 source/problem/testdata → 本地真实编译（g++）并运行全部测试数据 → `POST /judge/results` 回传终态（真实 time_used/memory_used）、失败回传 compile-info/run-info 与 judge_error/system_error；心跳 heartbeat。
- **范围**：新增 `judge/judge_daemon.py`；后端 `FAKE_JUDGE=false` 分支；`judge/README.md` 启动方式。
- **输出（验收断言）**：`FAKE_JUDGE=false` 下按 `DEMO_GUIDE.md` 步骤 2.6/2.7 复跑——A+B 正确解→accepted 且耗时/内存为真实值；错误解→wrong_answer；评测机日志可见真实编译运行记录；`FAKE_JUDGE=true` 仍可回退演示。
- **拆步**：① daemon 骨架+登录+心跳 → ② 拉任务/checkout/取数据 → ③ g++ 编译与运行器（时限/内存捕获）→ ④ results 回传与失败分支 → ⑤ 后端开关切换与冒烟。
- [x] 完成记录：2026-08-24 已实现。工具链 w64devkit v2.9.1 落地 `judge/toolchain/`；`judge_daemon.py` 全链路（login→tasks→checkout→数据→编译→逐点运行→回传+心跳）；`FAKE_JUDGE` 默认 false；smoke 加 setdefault 适配（A-4）。自测：HTTP 链路断言通过 + 编译运行快速核对 ALL PASS；完整复跑留联调日。问题单见 REVIEW_NOTES A-1~A-6。

## 块 B：G2 真实 WASM 编译（前端 + 资源域）

- **约束**：不改 `runCode` 签名与 `RunResult` 结构；clang.wasm 预编译资源懒加载 + brotli 压缩（PROJECT_MEMO §1）；替换后 `VITE_FAKE_WASM` 默认关。
- **需求**：懒加载预编译 clang.wasm，浏览器内编译用户 C++ 源码并执行，返回真实 stdout/stderr；编译失败展示真实错误文本；资源加载有进度/失败提示。
- **范围**：`frontend/src/wasm/runtime.ts` 真实分支；clang.wasm 资源落地（docs 记录来源与构建脚本）；`.env.development` 开关置 0。
- **输出（验收断言）**：前端点"运行"输入 A+B 代码输出 `3`（非样例回显）且无"演示模式"标签；输入含语法错误代码展示真实编译错误；运行失败分支有提示；`VITE_FAKE_WASM=1` 可回退。
- **拆步**：① 获取/生成 clang.wasm 并确认体积与加载方案 → ② runtime.ts 真实编译运行链路 → ③ 错误/进度 UI → ④ 开关切换与真机验证。
- [x] 完成记录：2026-08-24 已实现。资源自托管 `public/clang/`（clang22/lld22/sysroot22.tar，经 ghfast.top 镜像下载）；运行时源码自托管 `src/wasm/vendor/`（改资源路径 `/clang/`，B-4）；`runtime.ts` 真实分支（懒加载 Worker + 错误翻译）；`.env.development` 置 0。自测 `verify_clang_wasm.mjs` **6/6 PASS**（编译→链接→运行 A+B 输出 3；语法错误诊断）。浏览器端实测留联调日（B-6）。问题单见 REVIEW_NOTES B-1~B-7。

## 穿插小项（等待/间隙完成，均不阻塞 A/B）

- **G3 轮询退避**（P1）：`SubmissionsView.vue` 轮询间隔按 2s/4s/8s 递增（现固定 2s）。
- **G4 v-loading 警告**（P2）：移除 ElDialog 上的 v-loading，控制台 0 条警告。
- **G6 前端节流提示**（P1）：提交后 5s 内按钮禁用并倒计时，42901 友好转译。

## 执行细化（第 6 天上午落定）

### 工具链决策（环境探测结果）
- 本机 Windows：**无 g++/clang/docker/wsl**；winget 与网络可用。
- 块 A 编译通道：下载便携 w64devkit（GCC 16.2，58.6MB，`skeeto/w64devkit` v2.9.1）→ `judge/toolchain/`，daemon 通过 `CXX` 环境变量探测（默认 `toolchain/bin/g++.exe`，找不到则清晰报 judge_error 并降级）。
- 块 B 资源：下载 `cppstudio-io/wasm-clang-runtime` v0.1.0 三件套（clang22 34.4MB + lld22 18.3MB + sysroot22.tar 38MB）→ `frontend/public/clang/`，浏览器内 clang 22.1.8 编译 C++、lld 链接、sysroot 提供 libc++ 头文件。
- 资源均**自托管静态文件**，不引入新运行时依赖（忠于第 2 天选型"预编译 clang.wasm 优先"）。

### 块 A 文件清单与自测
- 改动：`judge/judge_daemon.py`（新增，核心）、`judge/README.md`（启动方式）、`judge/scripts/test_judge_daemon.py`（新增，自测）、`backend/app/main.py`（`FAKE_JUDGE=false` 时关闭假评测线程）。
- 自测命令（可重复）：`cd backend && python -m scripts.smoke`（契约冒烟）+ `cd judge && python scripts/test_judge_daemon.py`（daemon 全链路）。
- 用例：正常=正确解→accepted+真实耗时；坏输入=语法错误→compile_error+compile-info、错误答案→wrong_answer、未知语言→judge_error、无编译器→降级报错。

### 块 B 文件清单与自测
- 改动：`frontend/src/wasm/runtime.ts`（真实分支）、`frontend/public/clang/`（资源）、`frontend/scripts/verify_clang_wasm.mjs`（新增，Node 端自测）、`frontend/.env.development`（开关置 0）。
- 自测命令（可重复）：`cd frontend && node scripts/verify_clang_wasm.mjs`（加载 clang22 编译 A+B 并运行，断言输出 3；坏输入=语法错误断言编译失败）。
- 浏览器端验收：`VITE_FAKE_WASM=0` 下 `npm run dev`，题目页点"运行"，A+B→3 且无"演示模式"标签。

### 问题单（交叉阅读记录）约定
- 每块自测后生成 `docs/REVIEW_NOTES.md` 追加条目：**位置（文件:行）、现象、建议、是否已修**，供第二人（第 7 天联调人）审阅并发现问题。

## 说明

- 今天不做：管理端/细粒度权限（G5，第 7 天）；前端自动化测试（G7，第 8 天）；favicon（G8，第 8 天）。
- 风险预案：g++ 在评测机不可用→改用 mingw/容器内编译并记录；clang.wasm 体积过大→确认 brotli 后体积，超限则降级为"仅编译校验"并在差距清单注明。
- 若 A/B 任一阻塞，先完成另一方并如实更新 gap_list 状态，不假装成功。

# 1 实验报告02-在线评测系统web端开发

**报告编号：1 实验报告02-在线评测系统web端开发**
**完成方式：独立完成**（与 AI 助手的协作记录见第 7 节）
**日期：2026-08-20　配套文档：《模块图与选型比对.md》**

---

## 1. 基本信息

课题编号：1；课题名称：在线评测系统 web 端开发；报告编号：1 实验报告02；完成方式：独立完成。本日工作在昨日建设方案基础上，产出系统模块图、模块职责表与技术选型比对表，为第 3 天开始的编码分工提供依据。

## 2. 今日目标

第 1 天解决了"要做什么"，今天回答"**由谁做、做成什么、怎么协作、用什么技术做**"。具体目标：

1. 把系统拆成可分工的组成部分，明确每部分的职责与输入输出；
2. 说明各部分如何对应第 1 天确定的 6 项硬性需求（R1~R6）与主路径；
3. 对每个关键技术点做双方案对比，论证入选理由、放弃理由与风险应对。

最终目标是让图上每个方框都能落到"某端某人开发"，避免只有架构图、没有分工。

## 3. 组成部分

系统分三层共 14 个模块，依赖方向为：前端只调用后端接口，后端各服务依赖 SQLite 数据层，评测机只依赖后端评测机接口（HTTP 轮询解耦）。

| 层 | 模块 | 职责 | 输入→输出（上下游） |
|----|------|------|---------------------|
| 前端 | 认证模块 | 登录注册、JWT、路由守卫 | 账号密码→token（依赖后端认证服务） |
| 前端 | 题库模块 | 题库列表、题目详情 | 题目接口→列表/详情（依赖题目服务） |
| 前端 | 编辑器+WASM 模块 | 浏览器内 clang 编译运行、本地自测 PASS/FAIL、交互演示 | 源码+样例→输出/编译错误/提交动作（依赖题库模块与 WASM 资源） |
| 前端 | 提交记录模块 | 状态徽章、耗时展示、指数退避轮询 | 批量状态查询→终态展示（依赖提交服务） |
| 后端 | 认证与用户服务 | 注册/登录、JWT 签发、权限校验 | 认证请求→token/用户资料（依赖数据层） |
| 后端 | 题目服务 | 题目 CRUD、测试数据上传 | 管理请求/文件→题目与数据（依赖数据层） |
| 后端 | 提交服务 | 建提交、状态机流转、重判 | 提交请求→submission_id、状态列表（依赖数据层） |
| 后端 | 评测机接口服务 | 任务拉取、原子检出、结果回传、心跳 | 评测机请求→任务/检出/终态写入（依赖提交与题目服务） |
| 后端 | 交互器服务 | 交互器上传、编译、版本管理 | 交互器源码→编译产物/version+1（依赖数据层与题目服务） |
| 后端 | SQLite 数据层 | 7 张核心表持久化 | ORM 调用→查询结果 |
| 评测机 | 任务拉取与回传 | 登录、轮询、检出、回传 | token→任务/结果（依赖评测机接口服务） |
| 评测机 | 编译 | g++ 编译选手代码/交互器 | 源码→可执行文件或编译错误 |
| 评测机 | 运行与比对 | 普通题逐点运行与比对 | 程序+测试数据→AC/WA/TLE/RE |
| 评测机 | 交互评测 | 交互器与选手程序管道互联 | 程序+交互器→accepted/wrong_answer |

## 4. 与需求的对应

| 第 1 天核心必做/主路径 | 承担模块 | 处理办法 |
|------------------------|----------|----------|
| R1 用户与权限 | 认证模块 + 认证与用户服务 | 完全对应 |
| R2 题目发布/提交/状态展示 | 题库模块 + 提交记录模块 + 题目/提交服务 | 完全对应 |
| R3 评测机接口 | 评测机接口服务 + 评测机 4 模块 | 完全对应 |
| R4 浏览器内 WASM 编译运行、显示 AC/WA | 编辑器+WASM 模块（本地）+ 提交服务（正式判定） | 完全对应 |
| R5 通信题管理 | 交互器服务 + 交互评测 + WASM 交互演示 | 完全对应；未选的深入模块（打印队列、设备管理）**移除本期范围** |
| R6 文档与演示 | docs/api.md、seed、启动脚本 | 作为交付物维护，不设运行模块 |
| 主路径：登录→看题→运行→提交→轮询→评测→终态 | 认证→题库→编辑器+WASM→提交服务→提交记录→评测机→提交服务 | 完全对应 |
| 方案预留项：多机分片/沙箱/比赛榜单/Go | — | **不建模块**；mod/total 仅保留协议参数 |

若出现需求无人承担或模块对不上需求，一律删除或移出本期范围，保证模块图与第 1 天需求严格一致。

## 5. 技术选型

采用第 1 天方案既定技术栈，每个关键点按"安装便利性 / 与课题实例接近度 / 问题能否一天内定位"三个维度做双候选比较：

| 技术点 | 入选 | 放弃（理由） | 入选风险→应对 |
|--------|------|--------------|----------------|
| 前端框架 | Vue3+Vite | React（生态大、取舍成本高，调试链路长） | 锁版本、官方模板初始化 |
| 后端框架 | FastAPI | Flask（文档/校验/异步需手动补齐，挤压工期） | 锁定 Pydantic v2 兼容版本 |
| 数据库 | SQLite | MySQL（需装服务配账号，演示期故障面大） | 原子 UPDATE+单写事务防并发竞争 |
| WASM 语言 | C++/clang→WASM | **Go（编译器未移植 WASM，无法浏览器内编译源码，不满足 R4）** | 预编译资源+懒加载+压缩；失败降级走后端评测 |
| 认证 | JWT | Session（需服务端存储与跨域 Cookie，排错面广） | 短有效期+bcrypt |
| 编辑器 | CodeMirror6 | Monaco（体积大、集成复杂） | 预置语言包，不做深度定制 |

结论：所有入选方案均为第 1 天方案既定选择，9 天内可调试、可演示；放弃方案均给出明确理由，并有风险应对表兜底。

## 6. 今日完成与自检

**已确定**：系统 14 个模块的划分与职责表（名称/输入/输出/依赖/负责人）；依赖方向与主路径协作流程；R1~R6 与主路径的逐条映射；6 组技术选型的入选与放弃理由及风险应对；"对不上需求即移除"的处理规则。

**尚未确定**：WASM 工具链资源的具体来源与版本（采用预编译 clang.wasm 还是自建）；7 张表字段最终明细；接口路径与 JSON 约定的最终清单；seed 数据细节——以上留待第 3 天编码时定稿。功能边界已定：不含比赛/榜单/沙箱/多机部署/打印队列/设备管理。

## 7. 问题、计划与 AI 沟通

**不成功的沟通**：请助手直接"生成完整系统架构"，助手给出覆盖负载均衡、缓存、监控等大量模块的完整图，粒度过细且超出第 1 天需求验收范围，无法直接使用。后续改为把"R1~R6 + 主路径"作为输入，要求"只保留对得上的模块、对不上的删除"，并约束"职责表必须含名称/输入/输出/依赖/负责人"，收窄范围后得到可用版本。

**有效的沟通**：将约束条件、文件范围和验收方式一次性写入提问，如"逐条对应第 1 天核心必做或主路径，出现对不上就写明处理方法""技术选型按 安装/接近度/一天内定位 三维度、双候选、写明入选与放弃理由"，得到可直接交付的文档，无需多次返工。

**明日安排**：按 M1 里程碑开工——确定 7 张核心表（users/problems/test_cases/submissions/compile_infos/run_infos/interactors/judge_machines）的字段明细与建库脚本；敲定 /api/v1/auth、/api/v1/problems、/api/v1/submissions、/api/v1/judge/* 的接口路径与请求/响应 JSON 约定；实现注册登录与 JWT 认证；预研 clang.wasm 资源来源与加载方案。
